# Rook Ceph on Minikube Configuration File
# Copy this file and customize as needed
# The installation script will read this file if present

# ============================================
# Rook Configuration
# ============================================

# Rook version to install (e.g., v1.18.7, v1.14.9, v1.15.0)
ROOK_VERSION="v1.18.7"

# Ceph version (image tag)
# The Ceph container image version to use (e.g., v17.2.6, v18.2.0)
# This will be used in cluster.yaml or cluster-test.yaml
CEPH_VERSION="v18.2.0"

# Rook Ceph namespace
# The Kubernetes namespace where Rook Ceph will be deployed
ROOK_CEPH_NAMESPACE="rook-ceph"

# Path to Rook source directory (for custom builds)
# Default: $HOME/dev/go/src/github.com/rook/rook
ROOK_SOURCE_DIR="$HOME/dev/go/src/github.com/rook/rook"

# Enable custom build from source (true/false)
# Set to true to build Rook operator from local source
USE_CUSTOM_BUILD="false"

# Custom image tag for locally built images
# Only used when USE_CUSTOM_BUILD=true
CUSTOM_IMAGE_TAG="local-build"

# Container runtime to use for building custom images (docker or podman)
# Only used when USE_CUSTOM_BUILD=true
# Default: podman
CONTAINER_RUNTIME="podman"

# OSD type (disk or pvc)
# - disk: OSDs use raw disks (requires MINIKUBE_EXTRA_DISKS)
# - pvc: OSDs use PersistentVolumeClaims
# Default: disk
OSD_TYPE="disk"

# ============================================
# Minikube Configuration
# ============================================

# Number of nodes in the Minikube cluster
# Default: 1 (single node)
# For production-like setup, use 3 or more
MINIKUBE_NODES=1

# Memory allocation per node (in MB)
# Minimum recommended: 4096 (4GB)
# For multi-node: 4096 or higher per node
MINIKUBE_MEMORY=4096

# CPU cores per node
# Minimum recommended: 2
MINIKUBE_CPUS=2

# Main disk size
# Format: 20g, 30g, 50g, etc.
MINIKUBE_DISK_SIZE="20g"

# Number of extra disks for Ceph OSDs
# Each node will get this many extra disks
# Recommended: 2 or more for better Ceph performance
MINIKUBE_EXTRA_DISKS=2

# Minikube driver (qemu, virtualbox, docker, kvm2, etc.)
# Default: qemu (recommended for Mac M1/M2)
# Note: qemu driver provides good performance and compatibility on ARM64
MINIKUBE_DRIVER="qemu"

# Minikube network (optional)
# Specify a custom network name for the minikube cluster
# Leave empty to use default network
# Example: MINIKUBE_NETWORK="minikube"
MINIKUBE_NETWORK=""

# ============================================
# Example Configurations
# ============================================

# Single Node Development Setup (default)
# MINIKUBE_NODES=1
# MINIKUBE_MEMORY=4096
# MINIKUBE_CPUS=2
# MINIKUBE_EXTRA_DISKS=2

# Multi-Node Development Setup
# MINIKUBE_NODES=3
# MINIKUBE_MEMORY=4096
# MINIKUBE_CPUS=2
# MINIKUBE_EXTRA_DISKS=2

# High Performance Setup
# MINIKUBE_NODES=3
# MINIKUBE_MEMORY=8192
# MINIKUBE_CPUS=4
# MINIKUBE_EXTRA_DISKS=3

# Custom Build with Multi-Node
# USE_CUSTOM_BUILD=true
# MINIKUBE_NODES=3
# MINIKUBE_MEMORY=8192
# ROOK_SOURCE_DIR="$HOME/my-rook-fork"
# CUSTOM_IMAGE_TAG="my-dev-build"
